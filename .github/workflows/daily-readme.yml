name: Update Daily README

on:
  schedule:
    - cron: '0 0 * * *'  # daily at midnight UTC
  workflow_dispatch:      # manual trigger

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Update README
        run: |
          node <<'EOF'
          const fs = require('fs');
          const file = 'README.md';
          if (!fs.existsSync(file)) {
            console.error('README.md not found');
            process.exit(1);
          }

          // Predefined quotes
          const quotes = [
            'Code is like humor. When you have to explain it, it’s bad. — Cory House',
            'Experience is the name everyone gives to their mistakes. — Oscar Wilde',
            'First, solve the problem. Then, write the code. — John Johnson',
            'Simplicity is the soul of efficiency. — Austin Freeman',
            'Before software can be reusable it first has to be usable. — Ralph Johnson',
            'Programs must be written for people to read, and only incidentally for machines to execute. — Harold Abelson',
            'Any fool can write code that a computer can understand. Good programmers write code that humans can understand. — Martin Fowler',
            'Debugging is like being the detective in a crime movie where you are also the murderer. — Filipe Fortes',
            'The best way to get a project done faster is to start sooner. — Jim Highsmith',
            'Code never lies, comments sometimes do. — Ron Jeffries'
          ];

          const quote = quotes[Math.floor(Math.random() * quotes.length)];
          let content = fs.readFileSync(file, 'utf8');

          // Replace daily quote placeholder
          content = content.replace(/<!--DAILY_QUOTE-->.*$/, '<!--DAILY_QUOTE--> ' + quote);

          // Replace last updated placeholder
          const now = new Date().toUTCString();
          content = content.replace(/<!--LAST_UPDATED-->.*$/, '<!--LAST_UPDATED--> Updated on: ' + now);

          fs.writeFileSync(file, content);
          EOF

      - name: Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --cached --quiet || git commit -m "Update daily dev quote and timestamp"
          git push
